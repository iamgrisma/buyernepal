## 2025-05-20 - Product Image Optimization
**Learning:** Adding `loading="lazy"` and `decoding="async"` to product list images is a low-hanging fruit for performance, especially on the homepage where many images might be loaded.
**Action:** Always check for list-rendered images and apply lazy loading if they are likely to be below the fold.
