# Bolt's Journal

## 2025-10-27 - Initial Setup
**Learning:** Found that the project structure separates the Worker entry point from the React app, but the React app is fully present in `src/`. `tsc` is missing in the environment, need to install dependencies.
**Action:** Proceeding with React optimizations (Code Splitting) as it's a high-impact, standard optimization for SPAs.
